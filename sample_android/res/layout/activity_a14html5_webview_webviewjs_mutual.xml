<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/parent_ll_share"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >

    <!-- <View -->
    <!-- android:id="@+id/line_view" -->
    <!-- style="@style/myaccount" -->
    <!-- android:layout_width="fill_parent" -->
    <!-- android:layout_height="0dp" -->
    <!-- android:layout_below="@+id/common_top_title" -->
    <!-- android:background="@drawable/my_account_line_horizontal" /> -->

    <ProgressBar
        android:id="@+id/advertise_pb"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="fill_parent"
        android:layout_height="2dp"
        android:indeterminateOnly="false"
        android:max="100"
        android:progressDrawable="@drawable/a14html5_webview_webviewjs_mutual_progress_bar_states" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent" >

        <WebView
            android:id="@+id/advertise_url_detail_wv"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
        <!--
        <TextView
            android:id="@+id/advertise_rule_time_score_tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="right"
            android:layout_margin="10dp"
            android:background="@drawable/bg_scan_red_amount"
            android:gravity="center"
            android:paddingLeft="5dp"
            android:paddingRight="5dp"
            android:textColor="#ffffff"
            android:textSize="10sp"
            android:visibility="gone" />
        -->
    </FrameLayout>

</LinearLayout>